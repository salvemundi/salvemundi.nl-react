import React from "react";
import { Alert } from "reactstrap";
import "./Home.scss";

const Home = () => {
    return <div className="container-home">
        <Alert color="primary">
            Salve Mundi alert
        </Alert>
    </div>;
};

export default Home;